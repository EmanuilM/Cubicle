<main>
  <h1>Browser</h1>
  <form action="/" method="GET">
    <input type="text" class="search" name="search" placeholder="Search...">
    <input type="number" name="from" class="difficulty" placeholder="Difficulty from...">
    <span>-</span>
    <input type="number" name="to" class="difficulty" placeholder="Difficulty to...">
    <input type="submit" value="search">
  </form>
    {{#if products}}
    {{#each products}}
    <div class="cube">
    <img class="cube" src="{{this.imageUrl}}">
    <p class="name">{{this.name}}</p>
    <p><span>Difficulty level:</span> {{this.level}}</p>
    <a class="btn" href="/cubes/details/{{this.id}}">Details</a>
  </div>
    {{/each}}
    {{else}}
    <h1>No cubes were found</h1>
    {{/if}}

</main>